{% extends "layouts.njk" %}

{% block head %}

<title>Rocketseat - Início</title>

{% endblock %}


{% block content %}

        <div id="wrapper">

            <img src="{{about.logo_url}}" alt="Logo da {{about.nome}}">

            <h1> {{about.nome}} </h1>

            <ul>
                {% for tec in about.tecnologias %}
                    <li>{{tec}}</li> 
                {% endfor %}
            </ul>

            <p> {{about.texto}}</p>

        </div>

        <div id="footer-links" class="links">

            {% for link in about.links %}
                <a href="{{link.url}}" target="/blank"> {{link.name}}</a>
            {% endfor %}

        </div>

{% endblock %}

